services:
  - type: web
    name: igrid-project-management-dashboard
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        value: igrid_lab_dashboard_session_secret_2026_auth
